import React from 'react';

function PrintReferences(props) {
    return (
      <div>
        this is the print references page. Supplier number: {props.number}
      </div>
    );
  }
  
  export default PrintReferences;