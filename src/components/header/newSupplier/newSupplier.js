import React from 'react';
import { Button } from 'react-bootstrap';

function NewSupplier() {
    return (
      <Button variant="outline-info" size="lg">Nuevo proveedor</Button>
    );
  }
  
  export default NewSupplier;